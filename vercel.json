{
  "buildCommand": "cd porfolio && npm run build",
  "outputDirectory": "porfolio/dist",
  "installCommand": "cd porfolio && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
